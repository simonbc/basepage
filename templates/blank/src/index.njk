---
layout: base.njk
---
<h1>{{ site.title }}</h1>
{% if site.tagline %}<p class="lede">{{ site.tagline }}</p>{% endif %}

<p>
  This is a blank Basepage — one plain, legible page and nothing assumed. It's the
  canvas. Tell your agent what you want ("make this a blog about climbing", "design it
  like a terminal", "add an about page") and it edits these files. Start by replacing
  this paragraph.
</p>
